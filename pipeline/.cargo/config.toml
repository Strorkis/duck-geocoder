# 同時に走らせる rustc の数。
#
# 既定は論理CPU数だが、1プロセスが数百MB〜1GB使うので、16並列だと
# このリポジトリの依存 (arrow/parquet/proj/ureq+rustls+ring) では
# 7.9GBのメモリを使い切ってスワップに落ちる。実際にロード15まで上がり、
# 49分かけても cargo test が終わらなくなった。
#
# `-j 4` を毎回付けるのは忘れるので、ここに書いて既定にする。
# メモリに余裕のある環境なら、コマンドラインの `-j` が優先されるので上書きできる。
[build]
jobs = 4
